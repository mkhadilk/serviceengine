{
  "service": {
   "name": "serviceengine",
   "version": "${serviceversion}",
   "commitId": "${SHORT}"
  },
  "components": [
   {
    "name": "consumerproducer",
    "version": "${componentversion}",
    "commitId": "${SHORT}"
   }
  ]
}